import { createContext } from "react";
//an empty central state has been created
const StoreContext = createContext();

//central state has been exported to be used by other comps
export default StoreContext;